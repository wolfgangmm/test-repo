<?xml version="1.0" encoding="UTF-8"?>
<project default="all" name="stonesutras-admin">
  <description>Administrative functions for stonesutras</description>
  <xmlproperty file="expath-pkg.xml"/>
  <property name="project.version" value="${package(version)}"/>
  <property name="project.app" value="stonesutras-admin"/>
  <property name="build.dir" value="build"/>
  <!-- Start it -->
  <target name="init" description="create build directory">
    <tstamp/>
    <mkdir dir="${build.dir}"/>
  </target>
  <!-- Release build -->
  <target name="deploy" depends="init" description="compile release build">
    <zip basedir="." destfile="${build.dir}/${project.app}-${project.version}.xar" excludes="**/${build.dir}/**, .existdb.json, .yo-rc.json, **/node_modules/**, **/reports/** "/>
  </target>
  <target name="all" depends="deploy">
    <tstamp/>
  </target>
</project>